package co.edu.uniquindio.eventos.pfeventosp2.Model;

public class PlataformaEventos {
    /*private static PlataformaEventos instancia;
    private Usuario usuarioAutenticado;
    //falta crear clases Usuario, Evento, Recinto
    private ArrayList<Usuario> listaUsuarios;
    private ArrayList<Evento> listaEventos;
    private ArrayList<Recinto> listaRecintos;

    private PlataformaEventos() {
        //Faltan crear repositorios de las clases
        this.listaUsuarios = UsuarioRepository.getInstance().getUsuarios();
        this.listaEventos = EventoRepository.getInstance().getEventos();
        this.listaRecintos = RecintoRepository.getInstance().getRecintos();
    }

    public static PlataformaEventos getInstancia() {
        if (instancia == null) {
            instancia = new PlataformaEventos();
        }
        return instancia;
    }


    public boolean iniciarSesion(String correo, String contrasena) {
        // Lógica para buscar el usuario en listaUsuarios y asignarlo a usuarioAutenticado
        return false;
    }

    // Getters y Setters necesarios
    public Usuario getUsuarioAutenticado() {
        return usuarioAutenticado;
    }

    public void setUsuarioAutenticado(Usuario usuario) {
        this.usuarioAutenticado = usuario;
    }*/
}
